This archive contains a Todo app with frontend (React) and backend (Node/Express) plus Docker setup.

- To run locally without Docker: follow instructions in the previously created README in the canvas.
- To run with Docker:
    1) docker-compose up --build
    2) Wait for services to start. Frontend: http://localhost:5173, Backend API: http://localhost:4000/api/todos
